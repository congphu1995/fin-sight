# Where the FastAPI backend lives. Used by next.config.ts to proxy
# /api/v1/* from the Next dev server to FastAPI, sidestepping CORS in dev.
# In prod, point Nginx/Cloudflare at FastAPI directly and skip this proxy.
API_PROXY_TARGET=http://localhost:8000

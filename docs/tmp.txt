Initiate project with first feature: User input a request, and it return an answer using LLM

Init base file for folder structure:
+ uv with:
    + python 3.12
    + SQLalchemy
    + alchemic for db migration
    + FastAPI
    + Pydantic
    + ruff with base config for check input

+ app/
    + api/v1: which content api for healhcheck and base chat (in routes)
    + schema:
    + core:
        + loggiing: struclog (and setting for dev and prod)
        + database:
        + llm: use gemini
    + dependencies: wiring file
    + models: input / output models for api
    + main.py: with lifespan for fastapi


+ .env .example.env
+ ....